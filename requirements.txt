# fi-agent runtime dependencies
# Install only inside the project virtual environment (.venv), see CLAUDE.md.

# Agent orchestration
langgraph
langchain-core
langchain-ollama

# Config & validation
pydantic
pydantic-settings
pyyaml

# Market data
yfinance
pandas
numpy

# News gathering
feedparser
trafilatura
httpx
rapidfuzz
python-dateutil

# Reporting
jinja2

# Email delivery. certifi is explicit rather than transitive: Python from python.org
# ships an empty CA trust store, so SMTP over TLS depends on this bundle being present.
certifi

# CLI
typer
rich
